export class CityTemp {
    public temp: number;
    public creationDate: Date;

    constructor(temp: number, creationDate: Date) {
        this.temp = temp;
        this.creationDate = creationDate;
    }
}